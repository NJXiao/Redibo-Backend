generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model test {
  id_test     Int     @id
  namet       String? @db.VarChar(100)
  descripcion String?
}
